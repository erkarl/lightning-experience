# Lightning Experience Extension
This folder is used to pack the Chrome extension.

It contains:
* content script bundle
* background script bundle
* dashboard script bundle
* ui script bundle
* manifest & image assets
